# Maintenance Ledger

Unresolved decisions, deferred work, active limitations, and the dated evidence behind them. Durable rules live in `AGENTS.md`, shared guidance, a skill, a script header, or a test; remove an item here once its rule has moved there.

## Active Limitations

- OpenCode permissions (source-checked 2026-09-04 on 1.18.27: `permission/index.ts`, `tool/shell.ts`, `tool/read.ts`, `tool/edit.ts`, `tool/write.ts`, `tool/grep.ts`, `tool/external-directory.ts`, `tool/apply_patch.ts`, `core/src/util/wildcard.ts`): no classifier or OS sandbox; read and edit subjects are worktree-relative, external subjects are the parent directory plus `/*` (an external rule naming a file never matches), the shell's file commands and grep raise only the external subject, `apply_patch` asserts each target before writing, the file tools ask `edit` after the external check, so a write outside the worktree needs both, while the shell's file commands ask only the external subject, and the `edit` allow for the app temp root is a relative pattern that also matches a `tmp/opencode` subtree elsewhere outside the worktree. Live-checked 2026-09-03: destructive Git and remote changes prompt, and `.git` edits, `~/.ssh` reads, and pushes are refused. Live-checked 2026-09-05 on 1.18.27: a sibling README under `~/Projects` reads without a prompt, an edit outside the worktree asks, the plugin denies a plain commit, a `spar-claude review` returns a reviewer id, and the skill inventory lists OpenCode's built-in `customize-opencode` and its `explore` and `general` subagents beside the harness's skills, Omarchy's two links, and the `auditor`. Recheck the `AGENTS.md` invariants when those sources change.
- OpenCode subagent delegation (source-checked 2026-09-04 on 1.18.26, `tool/task.ts`): the task tool checks the `task` permission against the requested subagent type and enforces `subagent_depth`, default 1, so a subagent cannot launch another; the `auditor` agent denies `task` as well. Recheck when that source changes.
- Claude Code user-level agents (observed 2026-09-04 on 2.1.260 (Claude Code)): a new agent under `~/.claude/agents` registered in the running session, but after its file was renamed the old name was dropped and the new one did not appear until a new session. Start a new session after adding or renaming an agent; recheck when the sub-agents docs mention live reload.
- OpenCode WebFetch (source-checked 2026-08-15 on 1.18.18, `tool/webfetch.ts`) has no SSRF boundary; sensitive reads and shell network stay separately restricted. Recheck when that source changes.
- The payload scanner recognizes a finite set of credential shapes; repository consent, not the scanner, is the disclosure decision.
- Codex sandbox (observed 2026-09-03 on 0.152.1): a literal denied workspace path creates an empty placeholder file wherever the path is absent, and a home-wide glob that matches inside an already denied directory or a runtime tree (`~/**/id_ed25519`, `~/**/.npmrc`) aborts or stalls startup. Profiles use `**/` globs in the workspace and deny directories, not their contents, under `$HOME`; recheck when the sandbox changes. Observed 2026-09-04 on 0.153.2 with `~/Projects` readable: `~/Projects/**/.npmrc` stalls startup because it matches inside `node_modules` trees, while the same glob for `.netrc`, `.pypirc`, and the four `id_*` names starts cleanly, so a project `.npmrc` stays readable by Codex; recheck with each sandbox change. Enforcement observed 2026-09-04 on 0.153.2 through `codex sandbox -P trusted-workspace -C <temp workspace> cat <path>`, which runs one command under the profile without the model: a fixture README under `~/Projects` reads, fixture copies of `.netrc`, `.aws/credentials`, and `.claude/.credentials.json` there are refused with EACCES, and a fixture `.npmrc` reads. Primary-session canary 2026-09-05 on 0.153.2: a sibling README under `~/Projects` reads without an approval request, and a request for `~/.ssh/config` is refused by the model on the guidance before any read.
- Codex skills (source-checked 2026-09-05 at openai/codex `8e4b7d3`, `exec-server/src/local_file_system.rs` walk and `ext/skills/src/loader/discovery.rs`): the walk skips every symlink that is not a directory and follows directory links under user and repository roots, and a skill is listed only from a regular-file `SKILL.md` entry, so each skill under `~/.agents/skills` deploys as one directory link; recheck when those sources change.
- OpenCode TUI (observed 2026-09-01 on 1.18.25): a selected model variant persists across restarts and overrides the configured effort; confirm xhigh in the selector when it matters.
- Claude Code status line (source-checked 2026-09-03 on 2.1.259, the newest release): the `rate_limits` payload carries only `five_hour`, `seven_day`, and the gateway `spend_limit`. The per-model Fable weekly window, tracked internally as `seven_day_overage_included` and shown by `/usage` as "Current week (Fable)", is not exposed, and the only client-side route to it calls the usage API with the token in the credential store, which the security boundaries forbid. Add a `fable:` segment after `7d:` when a release exposes that window; recheck when the status line docs or a changelog entry mention `rate_limits`.

## Open Decisions

- Whether an Omarchy-only Claude Code sandbox trial justifies tracked promotion. The untracked trial denies `~/.ssh`, `~/.aws`, and `~/.gnupg`, asks before `dangerouslyDisableSandbox`, checks command classification and repository writes, and needs a separate WSL behavior test. Upstream: anthropics/claude-code#43713, #26722, #54215. Decision input (2026-09-04): `bwrap` is installed here, and the sandbox's network default of localhost only would block the research fetches, `make refs`, `npm run build` font downloads, and `verify-published` that sessions rely on unless `sandbox.network.allowedDomains` lists each host; the trial's next step is a tracked allowlist and one session of ordinary work on Omarchy with it on.
- Whether Claude settings should define `fallbackModel`; current judgment is no, because a silent downgrade conflicts with the durable Fable alias.
- `features.js_repl = false` in the Codex template has no recorded reason; revisit when a task would benefit from the REPL.

## Deferred Work

- WSL host pass, for the agent to run in a session opened in the WSL clone, stopping at the first mismatch:
  - Confirm the host: `/proc/version` names Microsoft, and `readlink -f ~/.claude/settings.json` resolves into this clone.
  - `git status --short` and `git diff --stat`; if `codex/.codex/config.toml` carries local edits, show H the hunks and obtain a ruling before anything is discarded, keeping config content out of chat and logs.
  - `git pull --ff-only`. The pull retires `codex/.agents`, so the links `~/.agents/skills/{commit,publish,spar}/SKILL.md` dangle and `make clean` cannot recognize them: confirm each link's text ends in `codex/.agents/skills/<name>/SKILL.md`, remove those three links, then `make restow`: report the dangling links it removed, confirm the managed parents are real directories, expect the `agents` package to deploy `~/.agents/shared-guidance.md` and `--link-skills` to make each of the three skills one directory link into the clone, and expect the Codex config to be installed from the template as a mode-600 file carrying the `~/.agents/shared-guidance.md` and `~/Projects` read grants, after which H re-enables desktop-app plugins locally. Restart OpenCode, whose `instructions` path moved to `~/.agents/shared-guidance.md`.
  - `make verify`; confirm the three tools resolve through mise (`command -v claude codex opencode` under `~/.local/share/mise`), that the Codex sandbox can start its mise-installed binary, and that `spar-claude` resolves the Claude runtime outside the repository and the temp roots; confirm OpenCode's temp root is `/tmp/opencode`. `make restow` also installs `templates/hooks/commit-gate` under `~/.agents/hooks` and the host Codex config gains the `[hooks]` table and the `~/.agents/shared-guidance.md` read grant.
  - One live `spar-codex review` from Claude Code and one live `spar-claude review` from OpenCode, each with a small artifact, the second with the shell tool given the bridge's timeout.
  - Before the same `make restow`: `make clean` retires the dangling `~/.claude/rules/shared-guidance.md` link; delete the emptied `~/.claude/rules` directory, and delete the seven dangling `~/.local/bin/{commit-candidate,commit-apply,publish-bind,publish-verify,spar-claude,spar-codex,spar-payload-scan}` links by hand, which `make clean` no longer recognizes because the `agents` package has no `.local` entry. The existing `~/.claude/skills/<name>` directories and their `SKILL.md` links stay: they still resolve, and `--no-folding` keeps skill entries as real directories of leaf links. After the restow expect `~/.claude/CLAUDE.md` as a link, a `scripts/` directory of links inside the `commit` and `publish` skill entries, and the scripts, bridges, scanner, and `review-brief` reachable under `~/.agents/skills/*/scripts` through the skill directory links, with `make verify` reporting `review-brief` executable and each skill link resolving into the clone; then ask Codex to list its skills and expect `commit`, `publish`, and `spar` beside `eyrsync`.
  - After the same `make restow`, expect the `auditor` agent at `~/.claude/agents/auditor.md`, its shared charter at `~/.agents/agents/auditor.md`, and OpenCode's `agent.auditor` entry reading that charter, the retired `~/.config/opencode/skills` links removed by `make clean` and their emptied directories to be deleted by hand, the host Codex config carrying `personality = "none"` and the `[hooks]` table, and OpenCode reading skills through `skills.paths` and lightweight tasks through `small_model`; restart OpenCode.
  - In the eyrwsl clone, `make restow` and `make verify`: the first run of its host and clone guards and of `verify` folding in `lint` and `check`.
  - Report `claude --version`, `codex --version`, and `stat -c '%a %h' ~/.codex/config.toml`, then remove this item.
- Rename the skills with an `eyr` prefix so they cannot collide with built-in or plugin commands; the form is undecided, H preferring the joined `eyrcommit` over `eyr-commit`. The rename touches the skill directories in the `agents` package and their Claude Code and OpenCode symlinks, the OpenCode command wrappers, the Codex template read grants, the `tests/prepare-stow.sh` fixture, and every cross-reference; deploy with `make clean`, `make restow`, and `make verify`, then remove the emptied skill directories under `$HOME`.
- An approval marker for Claude Code: a hook on the selector tool writes a marker carrying the candidate's tree id into a directory the agent cannot write, and `commit-apply` refuses without a matching marker in Claude Code sessions. H accepted the branch-advance guarantee as the criterion on 2026-09-04; this marker is the cheap extra layer for the tool used most, with no equivalent in Codex or OpenCode, whose approval prompts are not tool calls.
- A Codex `auditor` role: `sandbox_mode = "read-only"` restricts only model-generated shell commands and is the legacy key the contracts test rejects beside `default_permissions`, so a role needs a read-only permission profile and must also disable `web_search`, apps, plugins, MCP servers, and nested agents; write it once those keys are confirmed against the config reference and verify the merged authority in a live Codex session.
- Give omasecboot a `check` target that runs `test`, so the gate contract covers it; wait until H's current work there is done.
- Behavioral evals for the skills through `claude plugin eval`: the first scenarios are that free text never commits, that a rejected message leaves no record, and that a push is presented only after the gates; this makes the harness's behavior evidence rather than prose.
- Run `/fewer-permission-prompts` against accumulated host transcripts and promote only durable read-only rules; keep `gh api` excluded.
- Revisit native cross-model review when a managed tool offers a read-only cross-vendor path under subscription authentication.
- Keep Codex-to-Claude review manual outside the strict Codex profile until its root-denied permissions can grant the Claude runtime and state without weakening the primary sandbox.

## Revalidation Triggers

- When a Claude Code release reads `AGENTS.md` or `~/.agents/skills` natively (announced by Anthropic in late August 2026 as "more hackable", without a date): run `/context` and `/skills` for a guidance file loaded twice or duplicate skill entries, then remove the root `CLAUDE.md` import shim or the `claude-code/.claude/skills` symlinks accordingly.
- In the first Codex session and the first OpenCode session on WSL after the commit gate landed: ask the tool to run a plain `git commit` and expect the denial. The fixture test proves the hook command, payload, and plugin call, not the vendor's own dispatch of them. On Omarchy both tools dispatched it on 2026-09-05: OpenCode's plugin denied an empty test commit with the gate's reason, Codex's PreToolUse hook blocked the same command with the gate's reason, and HEAD stayed in place both times. Record WSL here and remove this item.
- Run `/eyrsync` when a tool release changes a configuration key, hook event, agent or skill field, or permission rule, when OpenCode moves to a new major version, when the harness changes something a sibling repository depends on, or when no pass has run for a while; it reads these triggers and refreshes the dated evidence below.
- Routine client releases need no ledger update; revalidate when a dependent interface, permission rule, temp root, matcher, model catalog, or observed behavior changes. Evidence uses `claude --version` and `mise ls --current` so stateful wrappers are not invoked.
- Re-run `make check` after changing a bridge, the scanner, a permission profile, or their runtime dependencies; re-run one live review per bridge after changing reviewer flags or when `claude -p` or `codex exec` behavior changes.
- Re-run the Codex root-deny, workspace, network, plugin-isolation, and resumed-session checks after changing its permission profile or runtime boundaries; `codex sandbox -P <profile> -C <dir> <command>` tests the filesystem rules directly, since the model refuses credential-shaped reads on its own before the sandbox sees them.
- Re-run the OpenCode project-isolation, app-temp, and permission-subject checks after a relevant OpenCode change; the project-disabled launch in `README.md` depends on `OPENCODE_DISABLE_PROJECT_CONFIG` and `OPENCODE_DISABLE_EXTERNAL_SKILLS` continuing to exist.
