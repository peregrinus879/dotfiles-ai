# AGENTS.md - dotfiles-ai

This repo stores portable user-level AI assistant configuration for Claude Code, Codex, and OpenCode, deployed to `$HOME` via GNU Stow: the stowed `claude-code/`, `codex/`, and `opencode/` payloads, the shared cross-tool guidance file, and the commit workflow used by all managed tools. Auth, session state, machine-local files, and generated host-specific config stay out.

## Load Map

- Claude Code loads `~/.claude/CLAUDE.md` and `~/.claude/rules/*.md` (stowed from `claude-code/`), then this file through the root `CLAUDE.md` `@AGENTS.md` import.
- Codex loads `~/.codex/AGENTS.md` (stowed from `codex/`, a symlink chain to the shared guidance file), then this file natively as the repo-root `AGENTS.md`.
- OpenCode loads `~/.config/opencode/AGENTS.md` (stowed from `opencode/`) and the shared guidance file through `instructions` in `opencode.json`, then this file.
- Skills load on invocation only. Repo-root docs are not stowed.
- Repo-root `.claude/settings.json` and `opencode.json` are inert per-tool project config placeholders with no command grants.

## Invariants

- Shared cross-tool guidance is canonical in `claude-code/.claude/rules/shared-guidance.md`; extend it for new shared guidance and keep tool-specific mechanism in each tool's wrapper (share policy, separate mechanism). Codex consumes it through the `codex/.codex/AGENTS.md` symlink chain, never a copy.
- Sequence rule: every repo list, tree, and doc orders the tools Claude Code, Codex, OpenCode.
- Reviewer matrix (cross-vendor, subscription auth only): Claude Code spars with Codex via the stowed `spar-codex` bridge; Codex and OpenCode spar with Claude via the stowed `spar-claude` bridge; both bridges hard-code their safety flags, reject higher-precedence non-subscription auth, isolate reviewer customization, preflight subscription auth fail-closed, validate their timeout knobs (zero or malformed values abort), and own the private handoff lifecycle. `spar-codex` supplies one strict inline read-only profile to new and resumed calls; `spar-claude` exposes only `Read`, `Glob`, and `Grep`, with plan mode and the write-tool deny remaining defense in depth.
- When editing sibling dotfiles repos, use identical wording for shared concepts; only repo-specific values (scope, package lists, invariants) differ.
- Keep wrappers thin; detailed rationale goes in `README.md`.
- Known Limitations records repo decisions and behavior official docs do not state; doc-derivable facts (defaults, version gates, upstream status) are fetched at change time, not cached here.
- Commit boundaries never alter, stage, or temporarily revert unrelated hunks. A mixed file is deferred or escalated to H for a split decision.
- Phased-work approval authorizes the plan's listed commits unless H explicitly excludes commits; execution completes and commits one verified unit before starting the next. Trivial tracked work is one implicit atomic commit.
- Claude Code pins `permissions.defaultMode` `"auto"` deliberately. Native `Edit`, `Write`, and `NotebookEdit` asks remain deterministic; operator-based shell guards do not match in auto mode and rely on the recorded classifier backstop. OpenCode keeps the prompt-based `permission` map as its default and `--auto` stays a deliberate per-session choice, never a default, because it bypasses ask rules including `permission.edit`. The `autoMode` guardrails harden auto sessions and keep `"$defaults"` first in every list they set so the built-in rules survive.
- Codex uses the beta `reviewed-writes` permission profile: workspace read-only, only OS temp writable, network disabled, and `approvals_reviewer = "user"`. Persistent writes and network therefore cross the interactive approval boundary; `spar-codex` ignores ambient config and independently supplies a stricter read-only reviewer profile with handoff reads and sensitive-path denies.
- Codex, OpenCode, and `spar-codex` pin GPT-5.6 Sol Fast while retaining xhigh reasoning. The managed configuration assumes ChatGPT subscription auth, where Fast consumes GPT-5.6 credits at 2.5 times Standard; API-key use is separately billed and requires a separate configuration decision.
- Read `README.md` before structural changes; when the installed OpenCode binary and its docs disagree, prefer `/help` output and runtime behavior.
- Auth, session state, machine-local files, and generated host-specific files stay out of Git; keep the tracked nested payload ignore files aligned with the documented exclusions.
- Spar handoffs use one private mode-700 `/tmp/spar-<session-id>/` directory per session; both pinned read-only reviewers can read that exact directory under their existing sandboxes. The selected reviewer bridge creates the directory, revalidates ownership, mode, containment, symlinks, and link counts before every review call, and validates it again before cleanup.
- Every spar bridge call passes its prompt and complete handoff through the pinned `spar-payload-scan` before authentication or network access. The scanner rejects sensitive paths and common credential shapes; policy text that only names protected paths remains reviewable, and unrecognized secret formats remain a residual prose-gate risk.
- Both reviewer bridges require one valid terminal success event with a nonempty reply and own the reviewer process group. Stall and ceiling exits terminate TERM-ignoring descendants before the bridge returns.
- Global permissions are the trusted-repository default, not a boundary against project settings or plugins. Use the README's project-disabled launch commands for untrusted checkouts; verification proves those launches retain the global OpenCode write-review plugin.
- OpenCode plugin dependencies are pinned in the tracked npm manifest and lockfile at the installed OpenCode release; only generated `node_modules/` stays ignored.
- OpenCode in-app updates stay disabled because the external installation wrapper owns version selection; the tracked provider block contains only providers used by a managed model.
- Stow tree-folds directories that do not pre-exist at stow time into directory symlinks pointing at the repo. `make clean` keeps runtime-state parents (`~/.claude`, `~/.claude/skills`, `~/.codex`, `~/.agents`, `~/.agents/skills`, `~/.local`, `~/.local/bin`, and `~/.config`) real before stowing; absent `~/.config/opencode` may fold wholesale, while an existing real root receives managed child links (do not add `--no-folding`).
- Never weaken the sensitive-path deny rules; keep `~/.ssh` reads and `Bash(gh api *)` out of allowlists in both tools (H runs those via `!`).
- Sensitive-path Edit denies mirror unambiguous credential material only; `~/.ssh/**` and `./.env.*` stay ask-gated for the explicit-instruction exception, but deterministic `.env.*` read denies make agent editing impractical. Use `example.env` for editable placeholder templates.
- `statusline.sh` design conventions, including its intentional strict-mode omission, live in the script's header comment.
- `statusline.sh` persists hashed state names under one owner-validated mode-700 runtime directory and accepts only owner-only, regular, single-link state files. Unsafe root metadata disables all persistence; an unsafe state file disables only that entry, and neither is followed, repaired, or replaced.

## Post-Change Verification

- Run `make verify` and `make lint` from the repo root after changing the stowed payloads.
- After instruction or config changes, verify them in fresh Claude Code, Codex, and OpenCode sessions when practical.
- The full human checklist lives in `README.md` (Verify and Maintenance).

## Known Limitations

- OpenCode upstream shows a multi-file `apply_patch` as one approval dialog; the local `reviewed-writes.ts` plugin rejects grouped patches before permission, while the shared and OpenCode-local one-file instructions guide all managed tools (anomalyco/opencode#21914 closed not planned; linked PR #23262 remained open on 2026-08-14)
- rule order in the OpenCode `bash` permission map is semantic (rules are last-match-wins over per-command text including redirects): catch-all ask first, allows, then the redirect ask guards, denies last; `make verify` asserts no allow entry after the guard block
- the Claude Code redirect ask guards (`Bash(* >*)`, `Bash(* | tee*)`) mirror the OpenCode ask guards and stay ask-gated, never allowed; they match any command containing ` >` or `| tee`, including inside quoted arguments, so harmless matches prompt instead of hard-failing; live-verified 2026-08-08 on 2.1.226: they re-gate allowed commands in default mode, but in auto mode operator-based guards never match at all (redirects and pipe segments are invisible to ask matching; reported as anthropics/claude-code#85098) while text-matching guards like `Bash(*sed*-i*)` still prompt; shell write channels in auto mode therefore rest on the soft_deny prose alone, and `autoMode.classifyAllShell` stays pinned `true` so allowlisted commands reach the classifier instead of executing unexamined
- `disable-model-invocation` in skill frontmatter is Claude Code-only; the OpenCode commit skill has no equivalent gate and stays model-invocable
- skill `allowed-tools` frontmatter is Claude Code-only and pre-approves listed tools for the invoking turn rather than restricting them; the OpenCode commit skill keeps per-command prompts, and `make verify` allows that frontmatter diff between the copies
- the tracked `settings.json` pins `workflowSizeGuideline: unrestricted` deliberately (the app default is more restrictive); keep the pin
- do not add allow rules for Claude Code's built-in auto-run read-only Bash commands; they are redundant, and a blanket allow like `Bash(file *)` overrides the built-in re-prompt on write-capable flag forms (`file -m`)
- Claude Code writes app-managed state into `settings.json` through the stow symlink (its own key order plus internal keys like `skipWorkflowUsageWarning`); commit those rewrites as-is instead of reverting them
- the in-place-editor ask guards (`Bash(*sed*-i*)`, `Bash(*perl *)`, `Bash(*| perl*)`, `Bash(*awk*inplace*)`, mirrored as ask entries in the OpenCode guard block) share the redirect guards' substring matching and fresh-session verification note; combined short flags (`sed -ri`, `sed -Ei`, `sed -ni`, `ruby -i`) escape the globs and rely on the auto-mode soft_deny prose plus the shared-guidance ban on shell bulk edits
- auto-mode edit gating is layered deliberately: native edit asks and text-matching shell guards are the deterministic first line, while operator-based redirect and pipe guards do not match in auto mode; the two filesystem soft_deny prose lines are the probabilistic backstop. Plain `rm`, formatters, and writes hidden inside script files (`python gen.py`) are left to the classifier prose as a recorded scope decision, and session scratch deletion under `/tmp/claude-*` is a recorded `autoMode.allow` exception to the deletion line (repeated classifier blocks would pause auto mode during mandated cleanup)
- OpenCode `--auto` auto-approves every `ask` entry including `permission.edit` (only `deny` rules hold), so per-file edit review exists only in default OpenCode sessions; the mirrored guards harden default sessions only
- Codex permission profiles are beta in 0.147.0; native-Arch probes confirmed the tracked profile keeps the workspace read-only, permits `/tmp`, and blocks network. The profile inherits `:minimal` from `:read-only`; `glob_scan_max_depth = 64` bounds Linux/WSL deny-glob expansion and blocked a depth-9 `.key` canary. WSL remains a separate host validation.
- codex writes runtime state into `config.toml` (project trust entries, notice keys, MCP additions); rewrites flow through the stow symlink and are committed as-is (trust paths are portable across the identical-$HOME hosts); watch openai/codex#30045 for the MCP-write corruption class
- `codex exec` never prompts headless, so the sandbox is the only guard: the `spar-codex` bridge hard-codes a strict inline read-only permission profile, subscription and no-plugins preflights, ignored user config and rules, the `-c mcp_servers={}` override, a stall watchdog under an absolute ceiling, and fail-fast usage-limit classification; changes to those isolation controls or Codex permission semantics require re-verification
- every scripted `codex exec` carries `</dev/null` (non-TTY stdin hang, openai/codex#20919 class); `--last` is banned (not concurrency-safe); new and resumed calls select the same strict inline read-only permission profile through `default_permissions`
- codex skills live at `~/.agents/skills`, never `~/.codex/skills` (app-managed `.system`); `~/.local/bin/codex` and `~/.local/bin/opencode` are Omarchy npx `--prefer-online` wrappers (per-call startup tax and registry round-trip)
- headless `opencode run` had two live-verified failure classes on 2026-08-12 under 1.18.16: argv prompts above ~3.8 KB hung before session creation, and ask-gated external-directory reads hung forever (anomalyco/opencode#16367 class), while an ask-gated edit was auto-rejected; re-test rather than generalizing these results to another release
- spar runs keep repo-local writes review-gated in all three tools and use OS temp for handoffs. Fresh probes confirmed both pinned reviewer bridges can read a private `/tmp/spar-*` handoff without gaining write access. Codex can write its handoff automatically under the reviewed-writes profile; Claude Code and OpenCode keep handoff file writes ask-gated. The Claude-side first-round `SID=$(...)` capture still prompts because command-substitution forms never match allow rules (anthropics/claude-code#43713); both bridge scripts hard-code every safety-relevant flag.
- Claude Code 2.1.228 plan mode plus `--disallowedTools "Write Edit NotebookEdit Bash"` did not make the headless reviewer read-only: the built-in `Monitor` tool executed shell redirection and wrote a probe file. `spar-claude` now also pins `--tools "Read,Glob,Grep"`; fresh and resumed 2.1.228 bridge probes exposed exactly those tools and could not create files, and `make verify` statically pins the whitelist
- the spar reviewer stays frontier-tier and cross-vendor by design (a weaker cross-vendor reviewer measurably degrades a stronger author's output; arXiv 2607.21656: +18.1pp stronger, -8.6pp weaker); the charter deliberately never names the authoring model (source anonymity reduces identity-driven sycophancy, arXiv 2510.07517), and a same-model lens swarm was evaluated 2026-08-12 and rejected (correlated same-family errors yield few effective votes)
- `/doctor`'s confirmed cleanup deletes machine-local accumulated allowlists (`.claude/settings.local.json`) and rewrites `~/.claude.json` with backups rotating at five in `~/.claude/backups/`; after a cleanup, curate durable read-only rules into the tracked allowlist instead of restoring wholesale

## Deferred Items

- when anthropics/claude-code#85098 (our report; #42797 is the broader sibling) lands upstream, re-run the redirect and tee guard checks in a fresh auto session (operator ask guards should prompt again) and decide whether `autoMode.classifyAllShell` keeps its pin
- schema-level denies for `sudo` in both tools: blocked on whether permission rules also gate `!` bash-mode commands (undocumented); test in a fresh session with temporary deny rules (`! sudo -v`), then either land the denies or record that root checks are terminal-only
- Claude Code sandboxing: evaluated 2026-08-07 on the Omarchy host (bwrap and socat present, userns smoke test green), kept deferred. Auto-allow still prompts for `$VAR`, `$(...)`, and `VAR=` forms (anthropics/claude-code#43713), and it runs sandboxed in-repo writes (`sed -i`, `git commit`) without prompts, bypassing per-file review; shared enablement would also activate untested on WSL2 (leaked 0-byte files #26722, freezes #54215). Trial path when wanted: Omarchy-only via `/sandbox` (untracked `settings.local.json`) with `sandbox.credentials` denies for `~/.ssh`, `~/.aws`, `~/.gnupg` and an ask rule on `Bash(dangerouslyDisableSandbox:true)`; promote to tracked settings only after the trial and a WSL2 behavior test
- WSL host pass, a self-contained runbook for the session H opens at `~/Projects/repos/dotfiles`: 1) dotfiles-wsl: add the selected Codex installation method because the Omarchy npx wrappers do not exist there, then commit it in that repo; 2) dotfiles-ai: pull, compare and merge any real-file conflicts including Codex trust entries, run `make clean`, then `make stow` for a first install or `make restow` for an existing install, followed by `make verify` and `make lint`; 3) Claude Code: fresh-session shared-guidance and auto-mode gate checks; 4) Codex: verify ChatGPT auth, Sol Fast/xhigh, reviewed-writes workspace/temp/deny/network controls, WSL2 sandbox behavior, and `spar-codex` new/resume profile parity; 5) OpenCode: verify Sol Fast/xhigh, updater/provider ownership, tracked manifest/lock resolution and release match, global plugin loading, project isolation, and grouped-patch rejection; 6) bridge controls: payload scanner, auth isolation, private handoff symlink/hard-link checks, terminal success, timeout, and descendant cleanup; 7) fold outcomes back into this item
- run `/fewer-permission-prompts` against this host's accumulated transcripts and fold durable read-only winners into the tracked allowlist (deferred 2026-07-08 until the new host had real usage); keep `Bash(gh api *)` out per the allowlist invariant
- OpenCode's websearch tool stays gated to the OpenCode provider or an opt-in env var (`OPENCODE_ENABLE_EXA` or `OPENCODE_EXPERIMENTAL_PARALLEL`), so the tracked `websearch: allow` is inert with third-party models; decide whether to export an opt-in variable or keep the entry as future-proofing, and re-verify the gate against the installed binary at decision time
- decide whether the tracked `settings.json` adds `fallbackModel` (ordered fallback chain for provider overload; the highest-precedence file supplies the whole chain, no merge) for incident resilience
- `statusline.sh`'s hostname segment renders only under `SSH_CONNECTION` and may never render with no headless host; removal candidate on a future statusline-only pass, not bundled with unrelated changes
- watch native cross-model features in the managed tools for spar-relevant capabilities: gap analysis re-run 2026-08-12 (Claude Code agent teams experimental and Anthropic-only; OpenCode 1.18.x ships no native review), Codex adopted as the Claude-side reviewer the same day; re-run when any tool ships a native reviewer or cross-vendor path; both bridges are kept partly because they ride subscription auth rather than API-key billing
- if `claude -p` ever defaults to bare mode, re-verify the spar-claude bridge auth
- reverse-bridge validation: fresh Codex 0.147.0 under the tracked reviewed-writes profile launched `spar-claude` without an approval stop, but the bridge's Claude subprocess could not reach its API from the network-disabled sandbox and failed after 183 s with `ENOTIMP`; determine whether a command-specific network escalation can be pre-approved without broadening the default profile. The OpenCode side is complete and recorded above
- on the OpenCode v2 upgrade: re-verify the two headless hang classes, the bash permission matcher, build/plan `permission.edit` merge semantics, the `spar-claude *` allow, project-config isolation and global-plugin survival, release-matched plugin dependencies, Fast/model/provider/updater pins, and the grouped-patch runtime control, then update or drop the 1.18.x-dated entries here
- spar bridge visibility (tool-agnostic): live-watching headless reviewer rounds has no working path today; datum 2026-08-12: a standalone `opencode -s` TUI shows the session only as of open (the shared-server path `opencode serve` plus `run --attach` plus `opencode attach <url>` streams live but no longer hosts our reviewer), `codex resume <id>` opens a session interactively but does not follow a concurrent headless run; recheck when either tool ships session-follow

## Skills

- `/commit` - commit workflow with doc sync, scratch cleanup, staging, and push hint
- `/spar` - cross-model plan sparring: adversarial review rounds to evidence-based convergence, then execution and diff review; the counterpart tool is the read-only reviewer in a pinned headless session
