---
description: Sync existing docs with the current repo state
agent: build
---

Use the `update` skill for this repository.
Review the current implementation and update existing documentation files only where needed to reflect the current state.
