---
description: Review the exact commits a push, release, or pull request would expose
agent: build
---

Use the `publish` skill. Review the delta the publication would expose before presenting it as ready; H performs the push.
